# SQLite lives next to the code (file gets created automatically)
DATABASE_URL="file:./firewatch.db"

# Cognito (ID token verification)
COGNITO_USER_POOL_ID="us-east-1_XXXX"
COGNITO_CLIENT_ID="YYYYYYYYYYYYYYYYYYYYY"
COGNITO_REGION="us-east-1"

# MediaMTX
MEDIAMTX_BIN="mediamtx"                          # or absolute path
MEDIAMTX_CONFIG="/absolute/path/to/mediamtx.yml" # point to your config

# Cloud detection
FIRE_ENDPOINT="https://2cwzmjzkx4.execute-api.us-east-1.amazonaws.com/default/fire-frame-receiver"

# ffmpeg (must be installed)
FFMPEG_BIN="ffmpeg"

# Server
PORT=4000
